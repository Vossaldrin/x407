## What changed and why

## Verification
- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` passes
- [ ] Manually tested against a real hired agent (curl/browser) if a backend route changed
- [ ] No fabricated metrics or unimplemented-as-if-real claims in any copy changes

## Touches on-chain signing/verification code?
(`backend/x407.py`, `backend/dex.py`, `lib/x407-agent-pay.ts`, `lib/defi-swap.ts`) — if yes, explain what changed and how it was verified.
